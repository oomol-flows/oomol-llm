{
  "private": true,
  "scripts": {
    "build": "esbuild tasks/LLM/inputRender.jsx --bundle --format=esm --minify --outfile=tasks/LLM/bundled/inputRender.js",
    "build-entry": "esbuild tasks/LLM/main.ts --bundle --platform=node --format=esm --outdir=tasks/LLM/bundled"
  },
  "devDependencies": {
    "@ai-sdk/openai-compatible": "^0.1.12",
    "@oomol/types": "^0.0.9",
    "ai": "^4.1.47",
    "esbuild": "^0.25.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-select": "^5.10.1",
    "clsx":"^2.1.1"
  }
}
